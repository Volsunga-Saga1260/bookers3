<script src="https://cdn.jsdelivr.net/npm/raty-js@3.0.0/lib/jquery.raty.js"></script>
<div class="container">
  <div class="row mt-3">
    <div class="col-12 text-center">
      <h1>〜感想投稿一覧〜</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-3"> </div>
    <div class="col-sm-7"></div>
    <div class="col-sm-2 my-3">
      <%= link_to "新規投稿", new_movie_path, class:"btn btn-primary btn-block" %>
    </div>
  </div>
  
  <div class="row">
    <div class="col-12">
      <table class="table">
        <thead>
          <td><strong>投稿日</strong></td>
          <td><strong>感想タイトル：</strong></td>
        </thead>
        <% @movies.each do |movie| %>
          <tbody>
           <tr>
              <td><%= book.created_at %></td>
              <td class="align-middle" colspan="3"><%= movie.title %></td>
              <td class="col-sm-2 text-center">
                <%= link_to '詳細', movie_path(movie.id), class:"btn btn-info btn-block" %>
              </td>
            </tr>
            <tr>
              <td class="col-sm-8" colspan="3"><strong>原作：</strong><%= book.original %></td>
              <td class="col-sm-4 text-center"><strong>ジャンル：</strong></td>
              <td class="col-sm-4">
                <strong>投稿者名：</strong>
                <% if movie.customer == current_customer %>
                  <%= link_to movie.customer.name, customers_path(current_customer) %>
                <% else %>
                  <%= link_to movie.customer.name, customer_path(movie.customer.id) %>
                <% end %>
              </td>
            </tr>
          </tbody>
        <% end %>
      </table>
    </div>
  </div>
  <div class="my-3"><%= paginate @books %></div>
</div>